// The closing Contact view: "banded" keeps the full-bleed dark inverting
// band ("day becomes night"); "paper" stays on the light page background.
// Swapped on the preview, then the loser is deleted before merge.
export const CONTACT_GROUND: "banded" | "paper" = "banded";
